import { IdeaAnchorDirective } from './idea-anchor.directive';

describe('IdeaAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new IdeaAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
